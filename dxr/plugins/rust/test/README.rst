These are tests for the Rust compiler's emission of CSV analysis information.
They should move to the Rust compiler codebase once the CSV emission stuff is
merged. (Why aren't they in that branch of the compiler already?)
