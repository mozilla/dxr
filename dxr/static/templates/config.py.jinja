# Settings, filled out by dxr-build.py -f FILE

from ordereddict import OrderedDict

{#-
The right-hand side of these assignments will be filled using Jinja templating.
Using repr, we save simple variables this way for use later. Essentially, this
means that we don't do parser config files on the server, which is nice and
more speed.
#}

TREES                 = {{ trees }}
WWW_ROOT              = {{ www_root }}
GENERATED_DATE        = {{ generated_date }}
GOOGLE_ANALYTICS_KEY  = {{ google_analytics_key }}
DEFAULT_TREE          = {{ default_tree }}
ES_HOSTS              = {{ es_hosts }}
ES_ALIASES            = {{ es_aliases }}
MAX_THUMBNAIL_SIZE    = {{ max_thumbnail_size }}
